{
  "name": "bartender-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "Olena Domchevska",
  "license": "ISC",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && sls deploy",
    "deploy:p": "npm run build && sls deploy -f saveCocktail",
    "deploy:a": "npm run build && sls deploy -f getAllCocktails",
    "deploy:g": "npm run build && sls deploy -f getCocktailsByTaste"
  },
  "dependencies": {
    "aws-sdk": "^2.868.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.72",
    "@types/node": "^14.14.35",
    "serverless-plugin-typescript": "^1.1.9",
    "typescript": "^4.2.3"
  }
}
